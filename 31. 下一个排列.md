---
title: 31. 下一个排列
date: 2025-03-23
tags:
  - 双指针
---
### 问题描述

[题目链接](https://leetcode.cn/problems/next-permutation/description/?envType=study-plan-v2&envId=top-100-liked)

给定一个数组`nums`，找到其下一个排列

### 解题思路

#### 方法一

1. 先==从右向左==遍历，找到一个满足`nums[i] < nums[i+1]`的第一个位置
2. 从==i开始向右==遍历，找到第一个比`nums[i]`的位置j，交换`nums[i] nums[j]`
3. 反转`nums[i+1:end]`

```python
class Solution:
    def nextPermutation(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)

        i =  n - 2
        while i >= 0 and nums[i] >= nums[i+1]:
            i -= 1
        if i >= 0:
            j = n - 1
            while nums[j] <= nums[i]:
                j -= 1
            nums[i] , nums[j] = nums[j] , nums[i]
        

        left, right = i + 1, n - 1
        while left < right:
            nums[left], nums[right] = nums[right], nums[left]
            left += 1
            right -= 1
```

**算法复杂度分析：**

时间复杂度：$O()$

空间复杂度：$O()$
