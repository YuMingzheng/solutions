---
title: 215. 数组中第k个最大元素
date: 2025-10-11
tags:
  - 排序
---
### 问题描述

[题目链接](https://leetcode.cn/problems/kth-largest-element-in-an-array/description/?envType=problem-list-v2&envId=2cktkvj)

找到数组中第k个最大的元素，要求$O(n)$时间复杂度

### 解题思路

#### 方法一

（一段话解释算法的逻辑）

```python
class Solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        def quick_select(nums , k):
            pivot = nums[0]
            big , equal , small = [] , [] , []
            for num in nums:
                if num > pivot:
                    big.append(num)
                elif num == pivot:
                    equal.append(num)
                else:
                    small.append(num)
            if k <=  len(big):
                return quick_select(big,  k)
            if len(nums) - len(small) < k:
                return quick_select(small , k - (len(nums) - len(small)))    
            return pivot
        
        return quick_select(nums , k)
        
```

**算法复杂度分析：**

时间复杂度：$O(n)$

空间复杂度：$O()$
