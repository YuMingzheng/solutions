---
title: 35. 搜索插入位置
date: 2025-10-21
tags:
  - 二分法
---
### 问题描述

[题目链接](https://leetcode.cn/problems/search-insert-position/?envType=study-plan-v2&envId=top-100-liked)

给一个排序后的数组nums，和一个目标值target，找到target在nums中应该插入的位置

### 解题思路

#### 方法一

经典二分法

**while循环版本**
```python
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        left, right = 0, len(nums) - 1
        while left <= right :
            mid = (left + right) // 2

            if nums[mid] > target:
                right = mid - 1
            elif nums[mid] < target:
                left = mid + 1
            else:
                return mid
        return right + 1
```

**递归版本**
```python
def searchInsert( nums, low, high, target):
	if low > high:
		return low
	mid = (low + high) // 2
	if nums[mid] == target:
		return mid
	elif nums[mid] < target:
		return binarySearch(nums, mid + 1, high, target)
	else:
		return binarySearch(nums, low, mid - 1, target)
```

**算法复杂度分析：**

时间复杂度：$O()$

空间复杂度：$O()$
